LogicalInterface:
<LogicalInterface>:
    Button:
        text:'Press'
        # ação para quando clicar no botão
        on_press:root.OnPressing(Label)
        # acção para quando solta o click do botão
        on_release:root.OnRelease()
    Label:
        id: Label
        text: 'heloo'